# Quadratic Equation Solver

A lightweight C++ program that calculates the roots of quadratic equations with real coefficients using the discriminant method.

## ✨ Features

- **Interactive Input**: User-friendly coefficient entry (A, B, C)
- **Visual Calculation Process**: Shows step-by-step computation
- **Real Roots Detection**: Automatically determines if real roots exist
- **Multiple Cases Handling**:
  - Two distinct real roots (D > 0)
  - One real root (D = 0)
  - No real roots (D < 0)
- **Clean Interface**: Formatted output with precise decimal display

## 🚀 Getting Started

### Prerequisites
- C++ compiler (GCC, Clang, or MSVC)
- Windows OS (due to `system("pause")` and `system("cls")` commands)

### Compilation
```bash
g++ -o quadratic_solver diskr.cpp
